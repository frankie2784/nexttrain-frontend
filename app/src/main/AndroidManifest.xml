<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/widget_background"
        android:label="@string/app_name"
        android:roundIcon="@drawable/widget_background"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrainWidget">

        <!-- Widget Configuration Activity -->
        <activity
            android:name=".config.ConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.TrainWidget">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Settings Activity -->
        <activity
            android:name=".config.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.TrainWidget" />

        <!-- Widget Provider -->
        <receiver
            android:name=".widget.TrainWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="com.trainwidget.ACTION_REFRESH" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/appwidget_info" />
        </receiver>

        <!-- Update Service -->
        <service
            android:name=".widget.WidgetUpdateService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Alarm-based update receiver -->
        <receiver
            android:name=".widget.AlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.trainwidget.ACTION_ALARM_UPDATE" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
